{{>layout/header}}

<h1>회원 정보 수정</h1>
<div>
    <input type="hidden" id="id" value="{{user.id}}">
    <div class="form-group">
        <label for="username">ID</label>
        <p id="u-username">{{user.username}}</p>
    </div>
    <div class="form-group">
        <label for="nickname">닉네임</label>
        <input type="text" class="form-control" id="u-nickname" value="{{user.nickname}}">
        <span style="color: green">닉네임은 영어/숫자/한글 가능, 특수문자/공백 불가능, 3글자 이상 10글자 이하</span>
        <span id="ch-nickname"></span>
    </div>
    <div class="form-group">
        <label for="email">이메일</label>
        <input type="text" class="form-control" id="u-email" value="{{user.email}}">
        <button type="button" class="btn btn-success" id="btn-resend">인증</button>
        <span id="ch-email"></span>
    </div>
    <a href="/users/my-page" role="button" class="btn btn-secondary">취소</a>
    <button type="button" class="btn btn-primary" id="btn-update-user-info">수정</button>
</div>

{{>layout/footer}}