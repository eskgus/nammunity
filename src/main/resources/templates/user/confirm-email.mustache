{{>layout/header}}

<div>
    {{#error}}
        <p>{{error}}</p>
    {{/error}}
    {{#success}}
        <p>{{success}}</p>
    {{/success}}
    <button type="button" class="btn btn-primary" id="btn-close-window">확인</button>
</div>

{{>layout/footer}}